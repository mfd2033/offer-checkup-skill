# 04: 样例验证（动态选维 + 早停分支）

**What to build:** 在 `examples/` 增加一个「微型壳 / 主体混淆」样例，演示命中红线后仅跑 3 维即出高危报告的路径，兼作回归参照。

**Blocked by:** 02（依赖早停与缩减集逻辑已落地，才能写对应样例）

**Status:** done

- [ ] `examples/` 新增一个样例文件
- [ ] 样例展示 工商门命中红线（如 参保=0 或 主体混淆）→ 仅跑 参保 + 仲裁 + 套路 → 高危星级
- [ ] 样例标注「本样例验证动态选维 + 早停分支」，便于回归时对照
