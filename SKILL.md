---
name: offer体检
description: 招聘/公司体检技能。粘贴 BOSS直聘/智联招聘/猎聘/前程无忧/拉勾等招聘链接（或公司名），自动调用 browser-skill 爬取并交叉核验待遇、参保人数、劳动仲裁、招聘套路、网络/抖音/高德口碑，输出含 1–5 星推荐指数的单文件 HTML 报告。触发词：分析这家公司、招聘避坑、这家offer靠谱吗、公司体检、offer体检。
version: 1.4.0
---

# offer体检 · 招聘/公司体检技能

把一份招聘（或一家公司）做一次「入职前全面体检」：待遇是否匹配、社保是否真缴、有无劳动仲裁、招聘有无套路、网络/抖音/高德口碑如何，最后给出 **1–5 星推荐指数**，产出可双击打开的单文件 HTML 报告。

## 何时用

- 用户粘贴 **招聘网站链接**（BOSS直聘 / 智联招聘 / 猎聘 / 前程无忧 / 拉勾 等任一）
- 用户给出 **公司名称** 想评估
- 用户说：`分析这家公司` / `招聘避坑` / `这家offer靠谱吗` / `公司体检` / `offer体检`

## 前置依赖（强约束 · 不可用即停）

**`browser-skill`（命令前缀 `bsk`）是本技能唯一的页面抓取手段，全局禁用 `WebFetch`。** 页面「真正打开/抓取」一律走 `bsk`。各维度的来源站点与到达路径不自由发挥：按 `references/sources.md` 的**来源白名单 + URL 模板**用 `bsk navigate` 直连，`WebSearch` 退出来源发现路径（仅可用于实体消歧等非证据用途，搜索摘要不作数）。

1. **预检（preflight）**：技能一启动、识别输入的**同时**先做一次 `bsk session start`。链接模式与公司名模式**同受此门约束**。
2. **不可用即停**：若 `bsk` 命令不存在 / 扩展未加载 / `session start` 失败（即 browser-skill 整体不可用），**直接终止全流程**，向用户说明原因并提示「安装 / 登录 browser-skill 后重跑」；**不再发任何抓取调用，也不得回退 `WebFetch` 或请用户贴职位页文本**。
3. **停止边界（防过度终止）**：仅当 browser-skill **整体不可用**时才全局停。若 `bsk` 本身正常、只是某个具体页面撞登录墙 / 反爬，**不停全流程**——该页按「未核到（登录墙 / 反爬）」降级，其余维度照常并行出。

## 输入识别

1. 若输入含 `http(s)://` 且域名属于招聘站 → 走 **链接模式**
2. 若仅公司名/职位名 → 走 **公司名模式**
3. 两者都没有但用户在描述某家公司在招人 → 先向用户确认要分析的公司名或链接

## 链接模式（自动调 browser-skill 爬取）

优先用浏览器自动化绕过招聘站登录墙，拿到职位/公司原文：

1. 调用 `browser-skill` 技能（命令前缀 `bsk`）：
   - `bsk session start` 建立会话
   - `bsk navigate "<招聘链接>" --wait-until domcontentloaded` 打开页面
   - `bsk snapshot` 抓取页面文本/结构
2. 从快照提取：职位名称、薪资区间、工作地点、经验/学历要求、招聘方公司名、HR 名、福利列表、公司简介（**重点比对简介里的公司名是否与底部工商信息一致**）。
3. **browser-skill 整体不可用时不停在抓取、而在预检**：见「前置依赖（强约束）」——预检失败即全局终止并提示安装/登录 `bsk`，**严禁回退 `WebFetch`、严禁凭空编造、也不再请用户贴职位页文本**。若 `bsk` 正常但本页撞登录墙/反爬，则仅该职位页降级（标「未核到（登录墙/反爬）」），其余维度照常并行。

## 公司名模式

直接走下面的「页面取工商」：无职位页可解析，故改为直接发「`<公司名> 参保人数` + 主体一致性」作为**并行批次中的调用**（不串行预检），与其余维度同消息并发。

## 页面取工商（页面内联解析，非串行门）

链接模式抓取职位页后，**直接从页面文本解析**工商字段（主体 / 企业类型 / 规模 / 成立 / 注册资本 / 经营状态）。各站真实结构与「可直接解析字段 / 缺失字段」对照表见 `references/extraction.md`（其权威数据来源为 `tests/fixtures/job-pages/README.md` 与 `tests/test_extract_工商.py`，改 fixture 时同步更新该表）。

- **工商主体名**与**成立时间**：用于和招聘页简介主体比对（主体一致性）
- **企业类型**：是否自然人独资、是否微型壳（猎聘把「个人独资/自然人独资」直接缀在公司名后，可触发早停）
- **规模 / 注册资本 / 经营状态**：辅助判断壳公司风险
- **参保人数**：真实 JD 页不含参保人数（P0-2，见下），故默认走「**页面无参保 → 并行补查**」：把它作为下方并行批次中的**一个调用**补查（按 `references/sources.md` 参保维白名单查工商年报），**绝不前置成串行门**。仅当输入为企业工商详情页且页面已含参保时才直接采用。

> **P0-2（已知限制）**：`tests/fixtures/job-pages/README.md` 标注——所有招聘职位页（含 6 份 fixture）均不含"参保人数"（参保在工商年报/国家公示系统，不在 JD 页）。因此"页面已含参保 → 删工商调用"在真实 JD 输入下基本不可触发；本技能默认并行补查参保，不依赖该分支。
> 兜底：若页面完全无法解析上述字段（异常页/登录墙失败），回退「公司名模式」补一次工商查询，且该查询**并入并行批次**，不单独串行。

## 并行调研（单批并发 · 硬红线早停仅裁剪报告）

实体确认、工商字段已从页面取齐后，在**同一条消息**中并发发出全部所需维度（同消息全发，最大化并行、压低总时延）。

**维度分级（直接关系到总时延）**

- **硬红线维度（必发，全程）**：待遇 / 劳动仲裁 / 招聘套路 / 参保补查（页面无参保时）。四项决定安危，不可省，时延由其最慢一跳决定。
- **软口碑维度**：抖音口碑、高德口碑。多数公司无专属口碑、对结论权重低。
- **网络口碑**：居中（~1.5s）。
- **软维超时截断（实测得）**：软三维（网络/抖音/高德）单条 fetch 设 **~3s 墙钟截断阈值**——超过即中止该跳、该维按「未核到（超时截断）」降级并计入低覆盖，**绝不阻塞主报告**（其余维照常并行出）。据 2026-09-23 用 `bsk navigate` 实测软维口碑查询 9 条 ×3（n=27，含 RPC 往返）：p50≈1.4s、p90≈2.3s、p95≈2.8s、max≈3.75s，**>3s 仅 1/27**（抖音一条尾部），故 3s 只裁罕见长尾、几乎不误伤正常 fetch。此为 ADR-0002 决策 2「软维偶发长尾用超时截断 + 降级而非永久删除」的阈值固化。

**默认「全维」（时延均衡，推荐默认）**：待遇 / 劳动仲裁 / 套路 / 参保补查 / 网络口碑 / 抖音 / 高德 七维同消息并行发出。墙钟 = 最慢一跳，该跳是随机变量（可能是硬维度仲裁偶发 10s+，也可能是软维抖音偶发 5.6s），无法靠砍维度稳定压低；砍抖音/高德仅在软维恰好最慢时才见效，属偶发收益（ADR-0001 第 31 行明确砍软维「不在默认开启」）。压软维长尾的正解是给软维设 ~3s 超时截断（见上「软维超时截断」）把尾部封顶，而非默认砍维——前者保覆盖、后者丢覆盖。

**「标准版」（`--lite` 显式开启）**：在默认（全维）上砍掉 抖音 + 高德（五维），用于想提速且接受丢失软口碑覆盖的评估；硬维度变最慢极点时零收益。

> 用户可显式要求「标准版 / 砍抖音高德」提速，或「快速版只查三项」（待遇+仲裁+套路）进一步提速；默认即为全维，无需显式「深度版」。

每维**可查哪些站、按什么顺序降级、用什么 URL 模板与固定查询词直达**，单一事实来源全在 `references/sources.md`（来源白名单），本表不复制清单：只许查清单内站点，主站未命中/登录墙/反爬/模板失效才降下一备站，全部未命中则该维按「未获取到可核验来源」降级，**不得**换清单外站点找补。

| # | 维度 | 看什么 |
|---|---|---|
| 1 | 待遇 | 薪资区间、五险一金、加班/大小周、期权画饼；与同岗市场价对比 |
| 2 | 参保人数 | 参保人数=0/微型=高危；宣称规模 vs 工商实测 |
| 3 | 劳动仲裁 | 有无劳资纠纷、数量、性质 |
| 4 | 招聘套路 | 面试坑、薪资虚标、title 虚高、主体混淆、软文刷屏 |
| 5 | 网络口碑 | 在职离职评价两极、评分分 |
| 6 | 抖音口碑 | 员工/前员工爆料、负面集中点 |
| 7 | 高德口碑 | 办公环境、位置、实地评论 |

**来源台账（贯穿调研，供报告可点来源用 · 一视同仁）**：调研每个维度时同步维护结构化台账，每条为 `{维度, 平台名, 已打开URL}`。**统一硬标准**：凡要给出结论的维度，调研时**必须用 browser-skill（`bsk`）真正打开**至少一个来源页并记 URL（`WebFetch` 已全局禁用）；只拿到搜索摘要而未打开页面的，要么用 `bsk` 补打开、要么该维**不得下自信结论**、按「未获取到可核验来源」降级。**入账 URL 的域名必须落在该维来源白名单内**（`references/sources.md`），清单外页面的 URL 不得入账。**绝不凭记忆/猜测拼 URL，也不拿搜索入口链接充数**：入口 ≠ 证据页。产出阶段据台账渲染各段来源行（见「产出」）。

**硬红线早停（仅裁剪报告章节，不影响墙钟）**：命中任一条红线时，报告中**省略**低权维度（抖音/高德 + 网络口碑）章节，但已发出的调用不会撤回，故早停不减少调用、对时延零贡献 —— 它只服务于正确性与省 token：

1. 工商实测**参保=0** 或明确不缴；
2. **主体严重混淆**（简介主体 ≠ 工商主体）或 **自然人独资微型壳**；
3. **仲裁 / 投诉爆量**（黑猫大量投诉 + 公开仲裁集中）。

**交叉校验（关键）**：把「公司简介自称的主体/规模/成立时间」与「工商登记的主体/参保/成立时间」比对，任何不一致（如简介写 A 公司 2018 创立、工商是 B 公司 2022 年 0 人）都记为「主体混淆/壳公司」风险。

## 评分

按 `references/scoring.md` 的权重算法计算 1–5 星推荐指数。

## 产出

1. 用 `assets/report_template.html` 为模板，填入各维度结论与星级，并为**每个已跑维度**填来源行 `{{T1_SOURCES}}`–`{{T7_SOURCES}}`、头部填 `{{SOURCE_LABEL}}`（职位页友好标签，如「BOSS直聘·职位页」），生成单文件 HTML（内联 CSS，可双击打开）。
2. 把 HTML 交付给用户预览（如支持文件展示的 agent 可用 `present_files`），并附一段中文结论（核心发现 / 推荐星级 / 行动建议）。
3. 报告须包含各**已跑维度**的结论、星级与一句话推荐；因早停/限流/软维超时截断未跑的维度须标注「未跑（原因）」，不得留空或编造。
4. **运行标注（可观测性）**：在结论或报告末尾附一小段运行元数据——本次跑了哪些维度（全维 / 标准版(--lite) / 回退全维）、是否命中硬红线、是否触发限流回退、有无软维超时截断（~3s）。模板 footer 另固定填入 **技能版本号**（读自本文件 frontmatter 的 `version`，替换 `{{SKILL_VERSION}}`），便于溯源「这份报告由哪个版本产出」。纯展示，不影响星级算法。
5. **来源行渲染（对齐 ADR-0003 · 一视同仁）**：据「来源台账」渲染各段 `{{Tx_SOURCES}}`，规则：
   - **统一硬标准**：任何**下了结论**的维度，其来源行**必须**至少一条指向**本次真打开过**的页面 URL；**不分硬维/软维**，搜索摘要/未打开的返回 URL 一律不作数。
   - **格式**：以「来源：」起头，每条 `<a href="真URL" target="_blank" rel="noopener">平台名 · 定位标签</a>`（如「企查查 · 2025年报」）；一段可列多条，用顿号分隔。
   - **无合格来源即降级，不写“有结论+无链接”**：某维确实拿不到可打开的来源页时，**不得**摆自信分析段落——改按「证据不足」呈现：徽标用 `b-na`、`{{Tx_LEVEL}}` 写「未获取到可核验来源」、`{{Tx_TEXT}}` 正文说明原因（平台无公开页 / 登录墙 / 反爬 / 本岗无专项口碑），`{{Tx_SOURCES}}` 留空（模板 `.sources:empty` 隐藏）。
   - **未跑维度**：同上留空 + 标「未跑（原因）」。
   - 头部「主来源」= 职位页；顶部四张信息卡与第八段不挂来源行。

## 注意事项

- 数据缺失时标注「未获取到」，**不得编造**工商/仲裁数据。
- **选站不自由**：各维只查 `references/sources.md` 白名单内站点并按序降级；模板失效允许站内手动探索一跳（域名仍锁清单），不得借机出清单或回退 `WebSearch` 发现来源。
- browser-skill 整体不可用（预检失败）→ 全局终止并提示安装/登录后重跑，**不得回退 `WebFetch`、不得请用户贴文本**；`bsk` 正常但单页撞登录墙/反爬 → 仅该维度降级，其余照常。
- 抖音/高德若搜不到公司专属口碑，明确写「无有效口碑可参考」，不要硬凑；若单条 fetch 超 ~3s 则按「软维超时截断」中止并标「未核到（超时截断）」，不为等它拖慢整份报告。
- 涉及法律风险（仲裁/投诉）以公开权威源为准，标注来源与时间。
- **来源一视同仁、不得编造**：所有维度同一条硬标准——**下了结论就必须挂本次真正打开过的来源 URL**；拿不到就把该维降级为「未获取到可核验来源」（不摆自信结论），**严禁**凭记忆拼 URL、造「平台搜索入口」链接、或在结论旁标“（无链接）”蒙混。
- **默认即全维（含抖音/高德）**；用户显式要「标准版 / 砍抖音高德」才省去软维（偶发省时，硬维度变最慢极点时零收益）；「快速版只查三项」仍保留。命中硬红线时报告省略网络口碑段。
