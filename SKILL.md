---
name: offer体检
description: 招聘/公司体检技能。粘贴 BOSS直聘/智联招聘/猎聘/前程无忧/拉勾等招聘链接（或公司名），自动调用 browser-skill 爬取并交叉核验待遇、参保人数、劳动仲裁、招聘套路、网络/抖音/高德口碑，输出含 1–5 星推荐指数的单文件 HTML 报告。触发词：分析这家公司、招聘避坑、这家offer靠谱吗、公司体检、offer体检。
---

# offer体检 · 招聘/公司体检技能

把一份招聘（或一家公司）做一次「入职前全面体检」：待遇是否匹配、社保是否真缴、有无劳动仲裁、招聘有无套路、网络/抖音/高德口碑如何，最后给出 **1–5 星推荐指数**，产出可双击打开的单文件 HTML 报告。

## 何时用

- 用户粘贴 **招聘网站链接**（BOSS直聘 / 智联招聘 / 猎聘 / 前程无忧 / 拉勾 等任一）
- 用户给出 **公司名称** 想评估
- 用户说：`分析这家公司` / `招聘避坑` / `这家offer靠谱吗` / `公司体检` / `offer体检`

## 输入识别

1. 若输入含 `http(s)://` 且域名属于招聘站 → 走 **链接模式**
2. 若仅公司名/职位名 → 走 **公司名模式**
3. 两者都没有但用户在描述某家公司在招人 → 先向用户确认要分析的公司名或链接

## 链接模式（自动调 browser-skill 爬取）

优先用浏览器自动化绕过招聘站登录墙，拿到职位/公司原文：

1. 调用 `browser-skill` 技能（命令前缀 `bsk`）：
   - `bsk session start` 建立会话
   - `bsk navigate "<招聘链接>" --wait-until domcontentloaded` 打开页面
   - `bsk snapshot` 抓取页面文本/结构
2. 从快照提取：职位名称、薪资区间、工作地点、经验/学历要求、招聘方公司名、HR 名、福利列表、公司简介（**重点比对简介里的公司名是否与底部工商信息一致**）。
3. 若 `browser-skill` 不可用（未安装/无登录态）：回退 `WebFetch` 直接抓；若仍撞登录墙，**请用户把职位页文本粘贴过来**，严禁凭空编造。

## 公司名模式

直接走下面的「工商门预检」（已含 `<公司名> 参保人数 天眼查/爱企查` 等检索），预检完成后再进入并行调研。

## 工商门预检（必跑，动态选维与早停的主门）

无论链接模式还是公司名模式，只要**实体确认**拿到准确公司名，都先走本步，再进入并行调研。

用 `WebSearch`/`WebFetch` 查 **企查查 / 天眼查 / 爱企查** 的工商年报，提取并固化以下字段，作为后续动态选维与早停的**决策门（gate）**：

- **参保人数**（最新年报）：0 / 微型（<10）/ 与宣称规模是否匹配
- **工商主体名**与**成立时间**：用于和招聘页简介主体比对（主体一致性）
- **企业类型**：是否自然人独资、是否微型壳

> **限流兜底**：若三家工商源全部被限流/拿不到，标注「工商门未获取」，**不得编造**，并进入并行调研时的「回退全 7 维」分支（见下）。

## 并行调研（两波式 · 动态选维 · 硬红线早停）

### 波1：工商门（已由上节完成）
工商门结果决定波2 补跑哪些维度。

### 波2：按门结果同消息全发
看完工商门结果后，在**同一条消息**中并发发出波2 所需调用（同消息全发，最大化并行、压低总时延）：

- **门结果正常** → 补跑其余维度（待遇 / 仲裁 / 套路 / 网络口碑 / 抖音 / 高德），即全 7 维。
- **工商门未获取（限流兜底）** → **回退全 7 维**，保覆盖。

| # | 维度 | 主要数据源 | 看什么 |
|---|---|---|---|
| 1 | 待遇 | 招聘页原文 + 软文 | 薪资区间、五险一金、加班/大小周、期权画饼；与同岗市场价对比 |
| 2 | 参保人数 | 企查查/天眼查/爱企查 工商年报（工商门已取） | 参保人数=0/微型=高危；宣称规模 vs 工商实测 |
| 3 | 劳动仲裁 | 裁判文书网/企查查司法/启信宝/黑猫投诉 | 有无劳资纠纷、数量、性质 |
| 4 | 招聘套路 | 看准/职友集/脉脉/知乎 | 面试坑、薪资虚标、title 虚高、主体混淆、软文刷屏 |
| 5 | 网络口碑 | 百度/微博/贴吧/职友集评分 | 在职离职评价两极、评分分 |
| 6 | 抖音口碑 | 抖音搜索 `<公司名>` | 员工/前员工爆料、负面集中点 |
| 7 | 高德口碑 | 高德地图 POI | 办公环境、位置、实地评论 |

**硬红线早停**：命中任一条红线即**停止低权维度（网络口碑 / 抖音 / 高德）**，仅跑 **缩减集（参保 + 仲裁 + 套路 3 维）** 后直接出报告：

1. 工商实测**参保=0** 或明确不缴；
2. **主体严重混淆**（简介主体 ≠ 工商主体）或 **自然人独资微型壳**；
3. **仲裁 / 投诉爆量**（黑猫大量投诉 + 公开仲裁集中）。

**交叉校验（关键）**：把「公司简介自称的主体/规模/成立时间」与「工商登记的主体/参保/成立时间」比对，任何不一致（如简介写 A 公司 2018 创立、工商是 B 公司 2022 年 0 人）都记为「主体混淆/壳公司」风险。

## 评分

按 `references/scoring.md` 的权重算法计算 1–5 星推荐指数。

## 产出

1. 用 `assets/report_template.html` 为模板，填入各维度结论与星级，生成单文件 HTML（内联 CSS，可双击打开）。
2. 把 HTML 交付给用户预览（如支持文件展示的 agent 可用 `present_files`），并附一段中文结论（核心发现 / 推荐星级 / 行动建议）。
3. 报告须包含各**已跑维度**的结论、星级与一句话推荐；因早停/限流未跑的维度须标注「未跑（原因）」，不得留空或编造。
4. **运行标注（可观测性）**：在结论或报告末尾附一小段运行元数据——本次跑了哪些维度（全维 / 缩减集 / 回退全维）、是否命中硬红线、是否触发限流回退。纯展示，不影响星级算法。

## 注意事项

- 数据缺失时标注「未获取到」，**不得编造**工商/仲裁数据。
- 招聘站有反爬/登录墙，browser-skill 失败就回退搜索 + 请用户贴文本。
- 抖音/高德若搜不到公司专属口碑，明确写「无有效口碑可参考」，不要硬凑。
- 涉及法律风险（仲裁/投诉）以公开权威源为准，标注来源与时间。
- 维度由**工商门动态决定**，默认不预先裁剪；命中硬红线时只跑缩减集（参保+仲裁+套路）。旧的「快速版只查三项」仍可作为用户的显式要求保留。
